<% if object.errors.any? %>
<div id="error_explenation">
	<div class= "alert alert-error">
		The form contatin <%= pluralize(object.errors.count, "error") %>.
	</div>
	<ul>
		<% object.errors.full_messages.each do |msg| %>
		<li> <%= msg %></li>
		<% end %>
	</ul>
</div>
<% end %>